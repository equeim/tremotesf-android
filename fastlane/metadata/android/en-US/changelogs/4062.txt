Added support of adding multiple torrent links
Added sorting torrents by last activity
UI was rewritten using Compose framework. The design and UX remained mostly the same, however
Changed UI for selecting multiple items in a list. Selection panel is now shown at the bottom of the screen
Dialog for editing labels now shows a button for adding entered label (previously in only worked by pressing Enter on keyboard), as well as "no labels" placeholder to guide user to actually add the label
"Checking" filter now also shows torrents queued for checking
